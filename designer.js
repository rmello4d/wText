(function(widget) {

    widget.setWidth(50);
    widget.setHeight(16);

    widget.setPanelStyle({
        fClass: true, //This property is for the design panel
        text: true,
        background: true,
        border: true,
        sizePosition: true,
        label: true,
        textShadow: true,
        dropShadow: true,
        innerShadow: true,
        disabled: ['border-radius']
    });
});
